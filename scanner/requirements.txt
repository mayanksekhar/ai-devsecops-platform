# LangChain stack — bounded, let pip resolve compatible set
# Pinned to major.minor to prevent silent breaking changes
# Exact pins avoided for packages with complex native dependency chains
langchain>=0.3.25,<0.4.0
langchain-community>=0.3.24,<0.4.0
langchain-core>=0.3.60,<0.4.0
langchain-chroma>=0.2.4,<0.3.0
chromadb>=1.0.9,<2.0.0
ollama>=0.4.8,<0.5.0

# Test harness — exact pins, no native deps, safe to pin tightly
httpx==0.27.0
pytest==8.3.2
pytest-json-report==1.5.0

# PII detection
presidio-analyzer==2.2.354
presidio-anonymizer==2.2.354
spacy==3.7.6
